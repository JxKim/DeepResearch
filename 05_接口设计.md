# 接口设计

## 1. 设计目标

第一版接口只服务于 MVP 主链路：

- 创建研究项目。
- 自动生成研究任务书和大纲。
- 用户确认大纲。
- 触发研究报告生成。
- 查询任务状态。
- 查看报告结果。

接口设计原则：

- FastAPI 只负责请求接收、结构化校验、任务触发和结果返回。
- 长任务不在 HTTP 请求中同步完成。
- Agent 执行通过 FastAPI 后台任务触发，第一版使用 `asyncio.create_task`。
- 创建研究项目后，系统自动投递研究任务书和大纲生成任务。
- 所有核心结果都要有项目编号。
- 第一版不做登录和权限。

## 2. 接口列表

| 接口 | 方法 | 用途 |
| --- | --- | --- |
| `/health` | `GET` | 服务健康检查 |
| `/api/v1/research-projects` | `POST` | 创建研究项目，并自动提交研究任务书和大纲生成任务 |
| `/api/v1/research-projects/{project_id}/outline` | `GET` | 获取大纲草案 |
| `/api/v1/research-projects/{project_id}/outline` | `PUT` | 保存用户确认后的大纲 |
| `/api/v1/research-projects/{project_id}/report-tasks` | `POST` | 提交报告生成任务 |
| `/api/v1/tasks/{task_id}` | `GET` | 查询后台任务状态 |
| `/api/v1/research-projects/{project_id}/reports/latest` | `GET` | 获取最新报告 |

## 3. 创建研究项目

### 请求

```http
POST /api/v1/research-projects
```

```json
{
  "topic": "研究具身智能行业未来三年的机会",
  "research_goal": "判断公司是否需要关注该行业",
  "target_audience": "公司战略团队",
  "region_scope": "china",
  "time_scope": {
    "type": "recent_years",
    "years": 3
  }
}
```

字段说明：

- `region_scope`：地域范围，支持 `china`、`overseas`、`global`。
- `time_scope.type`：时间范围类型，支持 `recent_years`、`unlimited`。
- `time_scope.years`：当 `time_scope.type` 为 `recent_years` 时使用，表示近 N 年。
- 第一版请求中不提供 `knowledge_mode`。系统默认会查询内部知识库，但由智能体判断内部知识库结果是否和当前研究问题相关，是否进入最终报告。

### 响应

```json
{
  "project_id": "项目编号",
  "initial_task_id": "任务编号",
  "initial_task_type": "generate_research_brief",
  "topic": "研究具身智能行业未来三年的机会",
  "status": "brief_generating",
  "next_step": "wait_for_outline",
  "created_at": "2026-06-05T08:00:00Z"
}
```

说明：

- 创建研究项目后，后端自动创建大纲生成任务。
- 前端不需要再调用单独的大纲生成接口。
- `project_id` 是全局研究项目 ID，贯穿整个研究生命周期。
- `initial_task_id` 是创建项目后自动生成的初始后台任务 ID，用于查询“研究任务书和大纲草案生成任务”的状态。
- 一个项目后续可能包含多个任务，例如大纲生成任务、大纲修改任务、报告生成任务。
- 前端可以用 `initial_task_id` 查询任务状态。
- 大纲生成完成后，项目状态变为 `outline_ready`。

## 4. 获取大纲草案

### 请求

```http
GET /api/v1/research-projects/{project_id}/outline
```

### 响应

```json
{
  "project_id": "项目编号",
  "status": "outline_ready",
  "outline": [
    {
      "node_id": "1",
      "title": "行业定义和研究边界",
      "question": "本报告讨论的具身智能具体指什么",
      "description": "明确行业定义、研究对象和本报告不覆盖的边界",
      "children": [
        {
          "node_id": "1.1",
          "title": "行业定义",
          "question": "具身智能和传统机器人、通用人工智能有什么区别",
          "description": "说明核心概念、技术边界和典型应用形态",
          "children": []
        },
        {
          "node_id": "1.2",
          "title": "研究范围",
          "question": "本报告覆盖哪些地区、时间范围和应用场景",
          "description": "说明本次研究的地域范围、时间范围和场景边界",
          "children": []
        }
      ]
    },
    {
      "node_id": "2",
      "title": "市场规模和增长驱动",
      "question": "行业未来三年是否存在足够大的增长空间",
      "description": "分析市场规模、增速、核心驱动因素和不确定性",
      "children": []
    }
  ]
}
```

字段说明：

- `node_id`：大纲节点 ID，用于前端展开、定位和后续编辑。
- `title`：章节标题。
- `question`：该章节需要回答的核心问题。
- `description`：该章节的写作说明。
- `children`：子章节，支持多级嵌套。

## 5. 保存已确认大纲

### 请求

```http
PUT /api/v1/research-projects/{project_id}/outline
```

用户可以直接确认大纲，也可以用自然语言要求系统修改大纲。

直接确认大纲：

```json
{
  "action": "confirm"
}
```

要求系统修改大纲：

```json
{
  "action": "revise",
  "revision_instruction": "把竞争格局单独拆成一章，并增加头部公司的产品对比"
}
```

### 响应

直接确认大纲时：

```json
{
  "project_id": "项目编号",
  "status": "outline_confirmed",
  "next_step": "generate_report"
}
```

要求系统修改大纲时：

```json
{
  "project_id": "项目编号",
  "revision_task_id": "任务编号",
  "status": "outline_revising",
  "next_step": "wait_for_outline"
}
```

说明：

- `action=confirm` 表示用户不修改大纲，直接确认，项目状态变为 `outline_confirmed`。
- `action=revise` 表示用户用自然语言要求系统调整大纲，后端启动大纲修改任务，项目状态变为 `outline_revising`。
- 大纲修改完成后，项目状态重新变为 `outline_ready`。
- 前端继续调用 `GET /api/v1/research-projects/{project_id}/outline` 获取最新大纲。

## 6. 提交报告生成任务

### 请求

```http
POST /api/v1/research-projects/{project_id}/report-tasks
```

```json
{
  "user_instruction": "报告风格偏管理层汇报，结论要明确"
}
```

### 响应

```json
{
  "task_id": "任务编号",
  "project_id": "项目编号",
  "task_type": "generate_report",
  "status": "queued"
}
```

## 7. 查询任务状态

### 请求

```http
GET /api/v1/tasks/{task_id}
```

### 响应

```json
{
  "task_id": "任务编号",
  "project_id": "项目编号",
  "task_type": "generate_report",
  "status": "running",
  "message": "正在检索公开资料",
  "created_at": "2026-06-05T08:00:00Z",
  "updated_at": "2026-06-05T08:03:00Z"
}
```

## 8. 获取最新报告

### 请求

```http
GET /api/v1/research-projects/{project_id}/reports/latest
```

### 响应

```json
{
  "project_id": "项目编号",
  "report_id": "报告编号",
  "version": 1,
  "title": "具身智能行业机会研究报告",
  "html": "<html>报告正文</html>",
  "sources": [
    {
      "title": "来源标题",
      "url": "https://example.com/report",
      "published_at": "2026-01-01",
      "source_type": "public_web"
    }
  ],
  "created_at": "2026-06-05T08:30:00Z"
}
```

## 9. 为什么需要任务状态接口

研究任务不是普通接口查询，而是长时间后台执行。

任务状态接口用于支持：

- 前端展示排队中、执行中、完成、失败。
- 用户刷新页面后继续查看进度。
- 后端记录失败原因。
- 后续支持失败重试。

第一版不引入独立任务队列，后台任务由 API 进程内的 `asyncio.create_task` 启动。

任务状态接口仍然需要保留，因为前端不能依赖 HTTP 长连接等待研究完成。

后续如果引入 Celery 或 Dramatiq，接口结构不需要大改，只需要替换后台任务执行方式。

## 10. 第一版暂不实现的接口

第一版暂不实现：

- 单独提交研究任务书和大纲生成任务接口。
- 用户登录接口。
- 权限管理接口。
- 团队管理接口。
- 报告导出 PDF 接口。
- 内部知识库文件上传接口。
- 系统指标接口（获取当前有多少报告正在生成，当前生成了多少，消耗了多少token等）
- 研究项目总览和进度接口（前端调用获取当前的研究，有多少）

这些功能不影响主链路验证，后续再补。
